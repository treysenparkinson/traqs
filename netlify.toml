[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  timeout = 30

[dev]
  command = "vite"
  port = 8888
  targetPort = 5173
  autoLaunch = false

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
